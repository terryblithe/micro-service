server:
  port: 8060
spring:
  application:
    name: microservice-config-server-eureka
  cloud:
    config:
      server:
        git:
          uri: https://github.com/terryblithe/spring-cloud
          search-paths: config-repo
          username:
          password:
eureka:
  client:
    service-url:
      defaultZone: http://discovery:8761/eureka/
